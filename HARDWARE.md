# 实测硬件清单 (本项目开发者的完整配置)

本文档记录本仓库作者实际使用的硬件与环境,用于**复现、接线、排障**对照。
任何差异(不同开发板/屏/总线电压)都可能影响结果,请先对照本文档再提 issue。

---

## 1. 主机平台 (PC 端)

| 项目 | 实测值 |
|---|---|
| 形式 | 笔记本电脑 |
| 处理器 | **AMD Ryzen 7 8845HS**(Zen4 / Hawk Point, 8 核 16 线程) |
| 图形 | **AMD Radeon 780M**(核显, iGPU, 与 CPU 同 die) |
| 内存 | 2 × 12GB DDR5 **Crucial CT12G56C46S5.M4B1**(SPD Hub 温度可读) |
| 存储 | **KIOXIA-EXCERIA SSD** ×2(1TB [C:] + 另一块 [D:],SMART 温度可读) |
| 系统 | Windows 11(64 位) |
| 传感器工具 | LibreHardwareMonitorLib **0.9.6**(内置)、HWiNFO64 **8.52-6060**(可选,共享内存) |

> 该平台已实测:**CPU 真实温度 (Tctl/Tdie)、核显温度/占用、内存/SPD 温度、SSD 温度**
> 均可读;**风扇转速 LHM 与 HWiNFO 均无传感器**(0 项),属硬件限制,详见「已知限制」。

## 2. 开发板 — 合宙 AirM2M CORE ESP32-C3

| 项目 | 实测值 |
|---|---|
| 型号 | 合宙 **CORE-ESP32-C3**(正反面丝印 CORE-ESP32-C3 / AirM2M.CORE) |
| 芯片 | Espressif **ESP32-C3**(QFN32, 单核 RISC-V 160MHz, Wi-Fi + BT 5) |
| USB 串口 | 板载 **CH343**(USB-Enhanced-SERIAL CH343, **COM4**, VID_1A86/PID_55D3) |
| 原生 USB | 未使用(GPIO18/19 USB_DM/DP 由 CH343 方案替代,不影响本工程) |
| 板载 LED | **D4 = GPIO12**, **D5 = GPIO13**(高电平点亮) |
| I2C 引脚 | **GPIO4 = I2C_SDA(第 28 脚)**, **GPIO5 = I2C_SCL(第 27 脚)** |
| 供电 | USB-C 5V 输入,板载 LDO 输出 3.3V |
| 烧录 | 需 **按住板载 BOOT 键**再上传(自动下载电路不可靠,实测多次失效) |

**针脚对照(右侧 17~32,与本工程接线相关的 4 根):**

```
25 GND   26 3.3V   27 GPIO5 (I2C_SCL)   28 GPIO4 (I2C_SDA)
```

## 3. 显示屏 — LCD1602A(16×2 字符屏)

| 项目 | 实测值 |
|---|---|
| 型号 | **LCD1602A**(实际装配:黄绿背光黑字款;资料另含 3.3V 蓝屏版 / 5V 黄屏版) |
| 驱动 | **HD44780 兼容**(ST7066U 类控制器) |
| 规格 | 16 列 × 2 行,5x7 点阵字模(32 个 ASCII 字形 + 符号) |
| 供电 | 本工程按 **3.3V 统一供电**(资料内另有 3.3V 蓝屏版 / 5V 黄屏版可选) |
| 字符集 | ❌ **ROM 中无 ℃ 单字形**(有 °=0xDF);中文等非 ASCII 无法显示 |
| 对比度 | **板上蓝色电位器**(模块上)调节,屏亮无字时优先检查此处 |

引脚 16 针:VSS/VDD/VO(对比度)/RS/RW/E/D4~D7/A(背光+)/K(背光-),
由转接板通过 PCF8574 引出并复位,无需单独接线。

## 4. I2C 转接板 — PCF8574(LCD1602/2004 通用)

| 项目 | 实测值 |
|---|---|
| 型号 | **PCF8574 I2C 转接板**(55782 款, 支持 1602/2004) |
| 芯片 | **PCF8574**(I2C → 并口扩展) |
| 默认地址 | **0x27**(常见 0x27/0x3F,本工程固件启动时自动扫描) |
| 位序 | En=0x04, Rw=0x02, Rs=0x01, 背光=0x08, D4~D7=0x10~0x80 |
| 附加控件 | **蓝色电位器(对比度)**, 背光跳线(默认短接) |
| 供电 | 本工程模块整体接 **3.3V**(PCF8574 与 LCD 均支持) |

> ⚠️ 不要接 5V:ESP32-C3 的 GPIO 非 5V 容忍,3.3V 驱动是安全选择;
> 若你的屏必须在 5V 下才有足够对比度,请增加电平转换并单独供电。

## 5. 接线汇总(本工程标准接法)

```
PC (USB-C 线)
  │
  ├── USB 供电 + CH343 串口(COM4, 115200) ── 下载/同步用
  │
ESP32-C3                        PCF8574 转接板        LCD1602A
  ├── 3.3V  (26 脚) ────────────── VCC ─────────────── VDD
  ├── GND   (25 脚) ────────────── GND ─────────────── VSS/K
  ├── GPIO5 (27 脚, I2C_SCL) ────── SCL
  ├── GPIO4 (28 脚, I2C_SDA) ────── SDA
  └──                                     └── (板载排母插上 1602 的 16 针)
```

## 6. 软件工具链(实测版本)

| 工具 | 版本 | 用途 |
|---|---|---|
| Arduino IDE | 2.x | 固件编辑/上传(工具菜单: **esp32 → AirM2M CORE ESP32C3**, **USB CDC On Boot = Enabled**) |
| esp32 core | **3.3.11** | ESP32-C3 板卡支持 |
| LiquidCrystal_I2C | YwRobot 2011-05-17 修改版 | 1602 I2C 驱动库(随转接板资料提供) |
| LibreHardwareMonitorLib | **0.9.6** | PC 端传感器读取(MPL-2.0,构建时自动下载) |
| HWiNFO64 | **8.52-6060**(可选) | 详细传感器(共享内存接口,需手动安装启用) |
| csc(.NET Framework 4.8) | Windows 自带 | GUI/主机程序编译(**零安装**) |

## 7. 已知坑(全部为实测踩坑记录)

1. **烧录失败 "No serial data received"** → 芯片没进下载模式:按住 BOOT + 点 RST。
2. **LiquidCrystal_I2C.init() 会把 I2C 引脚重置回默认(8/9)** → 固件必须 `Wire.begin(4,5)` 后
   **直接调用 `begin()`**,不要调用 `init()`。
3. **AMD 移动 CPU Tctl 温度在 LHM 恒为 0**(开源社区已知问题) → 用 HWiNFO 后端读 `{temp}`;
   未启用时以 SoC 核显温度(`{soc}`)替代显示。
4. **本机风扇转速任何路径都读不到**(HWiNFO 282 项读数中风扇 0 项) → `{fan}`/`FAN ----` 是硬件限制。
5. **HD44780 CGRAM 位序 = 高位(bit4)在左** → 自定义字形若镜像,发送前 5 位反转
   (固件 `bitRev5()` 已处理,勿在 GUI 侧再反)。
6. **1602 无 ℃** → CGRAM 槽 1 自定义字形(模拟器与真屏同款)。
7. **USB 线数据线问题** → CH343 需要能传数据的 USB-C 线(部分充电线不可用)。
